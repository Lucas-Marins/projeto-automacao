Instalando awx tower

sudo apt update

sudo apt install ansible -y

curl -L https://github.com/d`ocker/compose/releases/download/1.21.0/docker-compose-$(uname -s)-$(uname -m) -o /usr/local/bin/docker-compose`

sudo chmod +x /usr/local/bin/docker-compose

sudo ln -s /usr/local/bin/docker-compose /usr/bin/docker-compose

sudo apt install -y nodejs npm

sudo npm install npm --global

sudo apt install python3-pip git pwgen vim

sudo pip3 install requests==2.22.0

docker-compose version  OBs: Checar versao do docker-compose e instalar igual no pip3

pip3 install docker-compose==1.29.2

wget https://github.com/ansible/awx/archive/17.1.0.zip

unzip 17.1.0.zip

cd awx-17.1.0/installer/

pwgen -N 1 -s 30   Obs: Guarda senha gerada e colocar no inventory

nano inventory

admin_user=admin 
admin_password=password 
create_preload_data=False 
secret_key=resultado do pwgen
*Se for rodar no docker, desmarcar opcao do projeto_Base = /var/lib/awx/projects



Criar playbooks dentro do container


// Alguns
* Se for rodar via doccker, tem que adicionar repo para baixar dependencias: https://techglimpse.com/failed-metadata-repo-appstream-centos-8/


